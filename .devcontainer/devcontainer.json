{
  "name": "Graph Data Science Workshop",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  
  "postCreateCommand": "bash .devcontainer/setup.sh",
  
  "postAttachCommand": "echo '\nLocal Neo4j + GDS (lessons 3.x): pre-set in .env as bolt://neo4j:7687\nNeo4j Browser: http://localhost:7474  (user neo4j / workshoppassword)\nAura (lessons 4.x): add your AURA_* credentials to .env\nVirtual environment ready at .venv — all dependencies installed\n\nNext steps:\n   1. Open the 3.x notebooks for the local-database lessons\n   2. Select kernel: Python (workshop-gds) if not auto-selected\n\n'",
  
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
        "python.terminal.activateEnvironment": true,
        "jupyter.notebookFileRoot": "${workspaceFolder}",
        "jupyter.kernels.filter": [],
        "notebook.defaultKernel": "${workspaceFolder}/.venv/bin/python",
        "files.exclude": {
          "**/__pycache__": true,
          "**/.pytest_cache": true
        }
      },
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-toolsai.vscode-jupyter-cell-tags",
        "ms-python.vscode-pylance"
      ]
    }
  },
  
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": true,
      "installOhMyZsh": true,
      "upgradePackages": true
    }
  }
}

