services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
    # app runs on the default compose network (NOT bound to neo4j's netns).
    # This gives it standard outbound DNS for the Aura lessons (4.x) while still
    # reaching the local Neo4j by its service name (neo4j:7687) for the 3.x lessons.
    depends_on:
      - neo4j

  neo4j:
    image: neo4j:5.15-enterprise
    container_name: workshop-neo4j
    environment:
      - NEO4J_AUTH=neo4j/workshoppassword
      - NEO4J_ACCEPT_LICENSE_AGREEMENT=yes
      - NEO4J_PLUGINS=["graph-data-science","apoc"]
      - NEO4J_dbms_security_procedures_unrestricted=gds.*,apoc.*
      - NEO4J_dbms_security_procedures_allowlist=gds.*,apoc.*
      - NEO4J_dbms_memory_heap_initial__size=512M
      - NEO4J_dbms_memory_heap_max__size=2G
      - NEO4J_dbms_memory_pagecache_size=512M
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j-data:/data
      - neo4j-logs:/logs
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider localhost:7474 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  neo4j-data:
  neo4j-logs:

